<hr/>
<div style="display: flex; align-items: center; gap: 1rem;">
  <img
    [src]="'https://render.albiononline.com/v1/item/' + weapon.uniquename + '.png'"
    width="64"
    height="64"
    alt="{{ weapon.uniquename }}"
  />
  <div>{{ weapon.uniquename }}</div>
</div>

<div *ngFor="let flatCraft of weapon.craftingrequirements; let i = index">
  <app-recipe [craftingrequirement]="flatCraft"></app-recipe>
</div>

<div *ngFor="let enchantCraft of weapon.enchantments?.enchantment; let i = index">
  craft nie jest poprawnie propagowany w dół :(
  <app-enchant-recipe [enchantment]=enchantCraft></app-enchant-recipe>
</div>
